<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
<h1><%= title %></h1>
<p>Welcome to <%= title %></p>

<table>
    <tr>
        <th>Username</th><th>ID</th><th>Title</th><th>Body</th>
    </tr>

    <% for (var i = 0; i < posts.length;  i++) { %>
        <tr>
            <td><%= posts[i].username %></td>
            <td><%= posts[i].postid %></td>
            <td><%= posts[i].title %></td>
            <td><%= posts[i].body %></td>
        </tr>
    <% } %>

    <% if (totalNum > 5) {%>
        <a id="next" href="<%='/blog/'+ username + '?start=' + nextid%>">Next Page</a>
    <% } %>
</table>
</body>
</html>